export default function PlatformBlockLabel({ children }: { children: React.ReactNode }) {
    return (
        <p className="text-sm font-medium tracking-tight">
            {children}
        </p>
    )
}